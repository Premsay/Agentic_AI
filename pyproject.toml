[project]
name = "ds-with-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "langchain-core>=1.6.2",
    "pydantic[email]>=2.13.5",
    "typing>=3.10.0.0",
]

[tool.uv.workspace]
members = [
    "03_Langchain",
]
